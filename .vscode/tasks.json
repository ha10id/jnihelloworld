{
    "tasks": [
        {
            "type": "shell",
            "label": "g++ build library",
            "command": "/usr/bin/g++",
            "args": [
                "-i'$JAVA_HOME/include'",
                "${file}",
                "-shared",
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}"
            ],
            "options": {
                "cwd": "/usr/bin"
            }
        }
    ],
    "version": "2.0.0"
}